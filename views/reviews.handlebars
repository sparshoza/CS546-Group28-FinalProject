<main>
  <div class="course-list">
    <h2 class="section-title">Course Reviews for {{courseCode}}</h2>
    <ol>
      {{#each course}}
      <li>
        {{this.reviewText}}
        <span>{{this.rating}}</span>
      </li>
      {{/each}}
    </ol>
  </div>
</main>